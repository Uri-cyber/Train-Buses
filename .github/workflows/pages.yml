name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master, claude/voxel-model-railway-qsar92]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Tries to create the Pages site; on repositories where the Actions
      # token is not allowed to do that, Pages has to be switched on once by
      # hand (Settings -> Pages -> Source: GitHub Actions) and this step is
      # then a no-op. Either way the build below still runs.
      - uses: actions/configure-pages@v5
        continue-on-error: true
        with:
          enablement: true
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Check the layout for clipping
        run: npm run check
      - name: Build
        run: npm run build
        env:
          BASE: ./
      - name: Also produce the single-file build
        run: SINGLE=1 npm run build && cp dist-single/index.html dist/standalone.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
