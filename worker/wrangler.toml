# How the live proxy is deployed. The GitHub workflow reads this file; nothing here is
# secret. The account id and the API token live in the repository's secrets.
name = "israel-by-rail-live"
main = "rail-live.js"
compatibility_date = "2026-09-06"

# keep the https://israel-by-rail-live.<subdomain>.workers.dev address the page already uses
workers_dev = true
